type User @entity {
  id: ID!
  firstInteractionTimestamp: BingInt!
  tokens: [UserToken]! @derivedFrom(field: "user")
}

type UserToken @entity {
  id: ID!
  user: User!
  token: Token!
}

type Token @entity {

}