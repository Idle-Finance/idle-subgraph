type User @entity {
  id: ID!
  firstInteractionTimestamp: BigInt!
  tokens: [UserToken]! @derivedFrom(field: "user")
}

type UserToken @entity {
  id: ID!
  user: User!
  token: Token!
  balance: BigInt
}

type Token @entity {
  id: ID!
  address: Bytes!
  name: String
}